import React from 'react'
import style from './index.css';


 const About = () => (
        <div className="about-section">
            <h1>Offline News Web-App</h1>
            <p>Offline News Web App build with React.js, React Router, Webpack4, Workbox, Service Worker, PWA, Intersection-observer</p>
        </div>
    )

export default React.memo(About);